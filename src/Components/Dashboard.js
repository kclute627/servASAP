import React, {useEffect} from 'react'

const Dashboard = () => {
    useEffect(()=> {
    
        window.scrollTo(0, 0)
      }, [])
    return (
        <div>
            {/* make buttons for ADD JOB NEW CLIENT   */}
            Dashboard
        </div>
    )
}

export default Dashboard
